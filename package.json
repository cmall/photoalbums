{
  "name": "albums",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "NODE_ENV=development concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "NODE_ENV=production npm run build -w client && npm run build -w server",
    "start": "NODE_ENV=production npm run start -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
